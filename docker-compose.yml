services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: sierra
      POSTGRES_PASSWORD: sierra
      POSTGRES_DB: sierraviva
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
